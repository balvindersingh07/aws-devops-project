# Deployment screenshots

Add your proof images here before final submission. Supported formats: `.png`, `.jpg`, `.jpeg`.

## Checklist

- [ ] **1-single-ec2** — at least 4 images
- [ ] **2-separate-ec2** — at least 5 images
- [ ] **3-docker-ecs-ecr** — at least 6 images

After adding files:

```bash
git add docs/screenshots/
git commit -m "Add AWS deployment screenshots"
git push origin main
```

Replace `YOUR_*` placeholders in the root [README.md](../../README.md) submission block with your real URLs.
